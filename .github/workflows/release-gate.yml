# =============================================================================
# ⚠️ DEPRECATED — This workflow has been replaced by ci.yml + release.yml
# =============================================================================
# Release gating is now handled by:
#   - ci.yml (runs on every push/PR — lint, test, security)
#   - release.yml (triggers on tag push, calls build.yml)
# The multi-gate pattern has been simplified.
# =============================================================================
name: '[Deprecated] Release Gate'
on:
  workflow_dispatch:
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See ci.yml + release.yml instead."
