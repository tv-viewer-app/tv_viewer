# =============================================================================
# ⚠️ DEPRECATED — This workflow has been replaced by build.yml + release.yml
# =============================================================================
# Build-release logic is now split: build.yml builds artifacts, release.yml
# creates GitHub Releases by calling build.yml via workflow_call.
# =============================================================================
name: '[Deprecated] Build Release'
on:
  workflow_dispatch:
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See build.yml + release.yml instead."
