# =============================================================================
# ⚠️ DEPRECATED — This workflow has been replaced by build.yml
# =============================================================================
# Android APK builds now run as the build-android job in
# .github/workflows/build.yml. APKs are uploaded as artifacts only
# (no longer committed back to the repo).
# =============================================================================
name: '[Deprecated] Build Flutter Android APK'
on:
  workflow_dispatch:
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See build.yml instead."
