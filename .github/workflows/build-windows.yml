# =============================================================================
# ⚠️ DEPRECATED — This workflow has been replaced by build.yml
# =============================================================================
# Windows builds now run as the build-windows job in .github/workflows/build.yml.
# =============================================================================
name: '[Deprecated] Build Windows'
on:
  workflow_dispatch:
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See build.yml instead."
