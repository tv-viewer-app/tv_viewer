# =============================================================================
# ⚠️ DEPRECATED — This workflow has been replaced by ci.yml
# =============================================================================
# All test, lint, and security checks now run in .github/workflows/ci.yml
# as parallel jobs with smart path-filtering.
#
# This file is kept empty to avoid GitHub showing a "removed workflow" warning.
# It will be deleted in a future cleanup.
# =============================================================================
name: '[Deprecated] Tests'
on:
  workflow_dispatch:
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See ci.yml instead."
