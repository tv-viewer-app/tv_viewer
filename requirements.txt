# TV Viewer Project Dependencies
python-vlc>=3.0.21203
requests>=2.34.2
aiohttp>=3.14.1
certifi>=2026.6.17   # CA certificate bundle for SSL pinning (Issue #97)
# pychromecast — OPTIONAL. Removed from default install because zeroconf (its
# transitive dep) is a known Windows Defender false-positive trigger and the
# Cast feature is opt-in. To enable Cast support: pip install pychromecast
customtkinter>=6.0.0  # Modern Fluent-style widgets (used by UI components)
# ttkbootstrap>=1.10.0  # Optional: enhanced UI styling (not required)
tkintermapview>=1.30  # OpenStreetMap map widget
Pillow>=12.2.0  # Image processing for thumbnails
defusedxml>=0.7.1  # Safe XML parsing — prevents XXE and billion-laughs attacks

# Web interface (optional — enables browser-based TV viewing)
fastapi>=0.138.1
uvicorn[standard]>=0.50.0
httpx>=0.28.1  # Required for FastAPI TestClient (starlette dependency)

# Build dependencies (for packaging)
pyinstaller>=6.21.0  # For creating standalone executables
